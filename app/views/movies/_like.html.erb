
 
 	<%if current_user.voted?      (review) %>
                  <%= link_to '<span class="glyphicon glyphicon-thumbs-up" style= "color:red;"></span>'.html_safe , review_vote_path(review_id: review.id ,user_id: current_user.id , id: current_user.votes.find_by(review_id: review.id)) , method: :delete , remote: true %></p>
                  
                  <% else %>
                   <%= link_to '<span class="glyphicon glyphicon-thumbs-up"></span>'.html_safe , review_votes_path(review_id: review.id) , method: :post , remote: 	true %></p> 
                   

              
                  <% end %> 
        <% if review.votes.count != 0 %>
         	<%= pluralize(review.votes.count , 'like') %> 
        <% end %> 	 

