<%= form_with(model: new_user, local: true) do |form| %>
  <% if new_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(new_user.errors.count, "error") %> prohibited this new_user from being saved:</h2>

      <ul>
      <% new_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :new_user_name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :new_user_email %>
  </div>

  <div class="field">
    <%= form.label :login %>
    <%= form.text_field :login, id: :new_user_login %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
